const makeUnique = (str) => {
    for (let x=0; x<str.length; x++) {
        if (str[x] === str[x]){
            false
        }else{
            true
        }   
  }
}
  console.log(makeUnique("Moonday"))

// Extra-Credit
// O(n)